# Backend (server) environment variables
# Set these in Vercel Project Settings → Environment Variables (do NOT commit real secrets)

# Primary (choose one style)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# Or individual fields
# DB_HOST=your-db-host
# DB_PORT=5432
# DB_NAME=your-db-name
# DB_USER=your-db-user
# DB_PASSWORD=your-db-password
# DB_SSL=true

# Auth/JWT
# JWT_SECRET=change-me
# JWT_EXPIRES_IN=7d

# CORS (frontend origin)
# CORS_ORIGIN=https://your-frontend-domain.vercel.app

# Optional
# LOG_FORMAT=combined
# RATE_LIMIT_WINDOW=15
# RATE_LIMIT_MAX_REQUESTS=100
# APP_VERSION=1.0.0
# NODE_ENV=production


# Frontend (React) build-time envs
# Use only REACT_APP_* keys — CRA exposes only those to the browser

# Point frontend to same-origin API routed by vercel.json/backend
REACT_APP_API_URL=/api/v1

# If you use Socket.io and host it separately, set this to that URL; otherwise omit
# REACT_APP_SOCKET_URL=https://your-backend-socket-host

# Optional
# REACT_APP_ENV=production

