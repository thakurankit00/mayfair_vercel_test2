# Frontend production build vars (Create React App)
# These are safe to commit (no secrets); only REACT_APP_* are exposed to the browser

# Use same-origin API under /api/v1 (routed to backend by vercel.json)
REACT_APP_API_URL=/api/v1

# If using Socket.io from a separate host, set it here, e.g. your production backend URL
# If omitted, client may fall back to localhost in dev; set explicitly if needed
# REACT_APP_SOCKET_URL=https://your-backend-host

