# Test Environment Configuration
NODE_ENV=test

# Database Configuration for Tests
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mayfair_test
DB_USER=test_user
DB_PASSWORD=test_password
DB_SSL=false

# JWT Configuration
JWT_SECRET=test-secret-key-for-jwt-testing-only
JWT_EXPIRES_IN=24h

# API Configuration
PORT=3001
API_VERSION=v1

# Redis Configuration (mocked in tests)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# AWS Configuration (mocked in tests)
AWS_ACCESS_KEY_ID=test-key
AWS_SECRET_ACCESS_KEY=test-secret
AWS_REGION=us-east-1
AWS_S3_BUCKET=test-bucket

# Email Configuration (mocked in tests)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test-password

# Twilio Configuration (mocked in tests)
TWILIO_ACCOUNT_SID=test-sid
TWILIO_AUTH_TOKEN=test-token
TWILIO_PHONE_NUMBER=+1234567890

# PayU Configuration (mocked in tests)
PAYU_MERCHANT_KEY=test-merchant-key
PAYU_MERCHANT_SALT=test-merchant-salt
PAYU_BASE_URL=https://test.payu.in

# Cloudinary Configuration (mocked in tests)
CLOUDINARY_CLOUD_NAME=test-cloud
CLOUDINARY_API_KEY=test-api-key
CLOUDINARY_API_SECRET=test-api-secret

# Test specific settings
TEST_TIMEOUT=30000
JEST_TIMEOUT=30000
